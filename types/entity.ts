export interface Work {
    title: string;
    image: string;
    video: string;
    date: string;
    description: string;
    skill: string;
    link: string;
}

export enum ArtworkType {
    WEB = "web",
    VIDEO = "video"
}
